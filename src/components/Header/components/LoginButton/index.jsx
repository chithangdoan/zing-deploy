import React from 'react';
import './style.scss';

const LoginButton = () => {
  return (
    <div className='login-button'>
      <button>Đăng nhập</button>
    </div>
  );
};

export default LoginButton;
